# Jpa
spring.jpa:
  hibernate:
    ddl-auto: create
  show-sql: true
  open-in-view: true
  properties:
    hibernate.format_sql: true

# Redis
spring.data:
    redis:
      host: localhost
      port: 6379

# Mysql
spring.datasource:
  url: jdbc:mysql://localhost:3306/princoin
  username: coinuser
  password: dptmzbdpfrhdqn3#
  driver-class-name: com.mysql.cj.jdbc.Driver

# Logging
logging:
  level:
    web: debug

# Quartz
spring.quartz:
    job-store-type: jdbc
    jdbc.initialize-schema: always
    properties:
      org.quartz.threadPool.threadNamePrefix: quartz-worker

# Security
jwt:
  secret: abcdefghijklmnOPQRSTUVWXYZ

spring:
  mail:
    host: smtp.gmail.com
    username: princoin000@gmail.com
    password: sdrkwzyhyvyewjam
    properties:
      mail:
        transport.protocol: smtp
        smtp:
          auth: true
          starttls:
            enable: true
            required: true